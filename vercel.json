{
  "buildCommand": "export PATH=/rust/bin:$PATH && cd telemetry-engine && cargo run && mkdir -p ../public && cp dashboard.html ../public/index.html",
  "outputDirectory": "public",
  "framework": null
}
